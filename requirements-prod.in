# Production dependencies for LUKHAS AI platform
# Use: pip-compile --generate-hashes -o requirements-prod.txt requirements-prod.in

# Include base dependencies
-r requirements.in

# Production monitoring & logging
structlog>=23.2.0,<25.0.0
sentry-sdk>=1.40.0,<2.0.0

# Production web server
gunicorn>=23.0.0,<24.0.0  # SECURITY: Fixed CVE-2024-6827, CVE-2024-1135

# Production database
# Note: Only install database drivers in production environment
# psycopg2-binary>=2.9.0,<3.0.0; platform_system != "Windows"  # Disabled for dev/test
redis>=5.0.0,<6.0.0

# Container health checks
healthcheck>=1.3.3,<2.0.0