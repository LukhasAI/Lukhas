title: test(core): add coverage for metrics.py
body: |
  # Test Request (ΛBot)

  **Target file:** `core/monitoring/metrics.py`
  **Tier:** 3
  **Rationale:** low coverage + high usage signal

  ## Requirements
  - Deterministic tests (freeze time, seeds; no sleeps)
  - Mock external IO (network, LLMs, stores)
  - Focus edge cases + error contracts
  - Coverage target: 70%

  ## Notes
  - Protected surface must not be edited.
  - Use the prompt in `prompts/labot/core_monitoring_metrics.md`.
