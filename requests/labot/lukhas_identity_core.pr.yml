title: test(lukhas): add coverage for core.py
body: |
  # Test Request (ΛBot)

  **Target file:** `lukhas/identity/core.py`
  **Tier:** 1
  **Rationale:** low coverage + high usage signal

  ## Requirements
  - Deterministic tests (freeze time, seeds; no sleeps)
  - Mock external IO (network, LLMs, stores)
  - Focus edge cases + error contracts
  - Coverage target: 85%

  ## Notes
  - Protected surface must not be edited.
  - Use the prompt in `prompts/labot/lukhas_identity_core.md`.
