# LUKHAS AI Environment Configuration Template
# Copy this file to .env and replace the placeholder values with actual secrets

# ============================================================================
# ⚠️  SECURITY WARNING: NEVER COMMIT ACTUAL SECRETS TO VERSION CONTROL
# ============================================================================

# === Required Security Keys ===
# JWT signing private key - REQUIRED for authentication
JWT_PRIVATE_KEY=your-jwt-private-key-here-at-least-32-characters

# OIDC issuer URL for identity federation
OIDC_ISSUER=https://your-oidc-provider.com

# Frontend origin for CORS policy
FRONTEND_ORIGIN=https://app.lukhas.ai

# === Required API Keys (Production) ===
# OpenAI API Key for LLM integration - REQUIRED in production
OPENAI_API_KEY=sk-your-openai-api-key-here-48-characters

# Anthropic API Key for Claude integration - Optional but recommended
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# Google API Key for Gemini integration - Optional
GOOGLE_API_KEY=your-google-api-key-here

# Perplexity API Key for search integration - Optional
PERPLEXITY_API_KEY=pplx-your-perplexity-api-key-here

# === LUKHAS System Security ===
# LUKHAS API key for internal authentication
LUKHAS_API_KEY=your-lukhas-api-key-here

# LUKHAS ID secret key - MUST be at least 32 characters
LUKHAS_ID_SECRET=your-lukhas-id-secret-key-at-least-32-characters-long

# Encryption key for sensitive data - MUST be exactly 32 bytes (44 chars base64)
ENCRYPTION_KEY=your-base64-encoded-32-byte-encryption-key-here

# === Database Configuration ===
# Primary database connection (PostgreSQL recommended for production)
DATABASE_URL=postgresql://username:password@localhost:5432/lukhas_db

# Redis for caching and session storage
REDIS_URL=redis://localhost:6379/0

# MongoDB for document storage (optional)
MONGODB_URL=mongodb://localhost:27017/lukhas

# === System Configuration ===
# Environment: development, staging, production
ENVIRONMENT=development

# Debug mode (automatically disabled in production)
DEBUG=true

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# === Feature Flags ===
# Enable consciousness processing system
CONSCIOUSNESS_ACTIVE=true

# Enable VIVOX consciousness system
VIVOX_ACTIVE=true

# Enable emotion processing
EMOTION_ACTIVE=true

# === Guardian System Configuration ===
# Ethics enforcement level: strict, moderate, lenient
ETHICS_ENFORCEMENT_LEVEL=moderate

# Guardian drift detection threshold (0.0 to 1.0)
GUARDIAN_DRIFT_THRESHOLD=0.15

# === External Services (Optional) ===
# GitHub token for automation (ghp_xxx format)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Azure client ID for Azure services
AZURE_CLIENT_ID=your-azure-client-id

# Azure tenant ID for Azure services
AZURE_TENANT_ID=your-azure-tenant-id

# ============================================================================
# Security Notes:
# ============================================================================
# 1. Generate strong secrets using: openssl rand -base64 32
# 2. Use different secrets for each environment (dev/staging/prod)
# 3. Store production secrets in secure vaults (AWS Secrets Manager, etc.)
# 4. Rotate secrets regularly (see scripts/security/rotate_secrets.md)
# 5. Never log or expose secrets in application output
# ============================================================================
