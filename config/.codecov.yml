codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        target: 75%
    patch:
      default:
        target: 75%

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: no
