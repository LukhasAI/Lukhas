[pytest]
minversion = 7.0
addopts = -ra --strict-markers --strict-config
testpaths = tests/smoke tests_new/reality/tier1 tests/golden
markers =
    matriz: MATRIZ pipeline tests
    golden: deterministic trace tests
    legacy: legacy test suite (temporarily excluded)
    no_mock: Reality tests without mocking