# Colon-separated absolute paths the server may read (and write if enabled)# Colon-separated absolute paths the server may read (and write if enabled)

LUKHAS_MCP_ROOTS=/Users/agi_dev/LOCAL-REPOS/LukhasLUKHAS_MCP_ROOTS=/Users/agi_dev/LOCAL-REPOS/Lukhas:/Users/agi_dev/Documents



# Optional: enable write tool (off by default)# Optional: enable write tool (off by default)

WRITE_ENABLED=falseWRITE_ENABLED=false



# === OAuth resource-server config ===# Optional: bearer token for authentication (leave empty for no auth)

# IMPORTANT: Point this to the OIDC discovery URL that returns JSON (the .well-known OpenID configuration).BEARER_TOKEN=
# Example (Auth0):  https://YOUR_DOMAIN/.well-known/openid-configuration
# Example (Azure v2): https://login.microsoftonline.com/<tenant>/v2.0/.well-known/openid-configuration
OAUTH_ISSUER=

# The API audience / identifier your IdP issues tokens for (e.g., api://lukhas-mcp)
OAUTH_AUDIENCE=

# The public base URL of this MCP server (scheme + host), used in PRM "resource"
PUBLIC_BASE_URL=