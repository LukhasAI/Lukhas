name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # LUKHAS Architecture Gates (REQUIRED)
  lane-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install tools (ripgrep + importlinter)
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ripgrep
          python -m pip install --upgrade pip
          python -m pip install import-linter
      - name: Enforce no candidate imports in lukhas
        run: |
          bash tools/ci/lane_guard.sh

  quarantine-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Check quarantine imports
        run: |
          echo "Checking for quarantine imports in production code..."
          if grep -r "from lukhas.quarantine" consciousness/ identity/ governance/ memory/ core/ bio/ labs/ 2>/dev/null; then
            echo "❌ ERROR: Found quarantine imports in production code!"
            exit 1
          fi
          echo "✅ No quarantine imports found"

  matriz-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies with constraints
        run: |
          pip install --upgrade pip
          if [ -f constraints.txt ]; then
            pip install -c constraints.txt jsonschema
          else
            pip install jsonschema
          fi
      - name: MATRIZ schema validation (examples + inbox if present)
        run: |
          python - <<'PY'
          import sys, subprocess, pathlib
          targets = []
          if pathlib.Path("MATRIZ/examples").exists():
              targets.append("MATRIZ/examples")
          if pathlib.Path("memory/inbox").exists():
              targets.append("memory/inbox")
          if not targets:
              print("✅ No MATRIZ directories to validate")
              sys.exit(0)
          cmd = [sys.executable, "-m", "MATRIZ.utils.matriz_validate", *targets]
          print("Running:", " ".join(cmd))
          result = subprocess.call(cmd)
          if result != 0:
              print("⚠️ MATRIZ validation failed (non-blocking during migration)")
          sys.exit(0)
          PY

  matrix-v3-sandbox:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies for Matrix v3
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Matrix v3 tokenization sandbox
        run: |
          echo "🪙 Testing Matrix v3 tokenization sandbox..."
          python3 tools/matrix_tokenize.py --contract contracts/matrix_identity.json --verbose
          if [ -f artifacts/token_anchor.json ]; then
            echo "✅ Tokenization artifact generated"
          else
            echo "❌ Tokenization failed - no artifact"
            exit 1
          fi
      - name: Matrix v3 provenance generation
        run: |
          echo "🔗 Testing Matrix v3 provenance generation..."
          python3 tools/matrix_provenance.py --contracts "contracts/matrix_*.json" --verbose
          if [ -f artifacts/provenance.car ] && [ -f artifacts/provenance_report.json ]; then
            echo "✅ Provenance artifacts generated"
          else
            echo "❌ Provenance generation failed"
            exit 1
          fi
      - name: Matrix v3 provenance verification
        run: |
          echo "🔍 Testing Matrix v3 provenance verification..."
          chmod +x tools/verify_provenance.sh
          tools/verify_provenance.sh --verbose
      - name: Matrix v3 smoke tests
        run: |
          echo "🧪 Running Matrix v3 provenance smoke tests..."
          python3 -m pytest tests/test_provenance_smoke.py -v --tb=short

  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Cache pre-commit
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ runner.os }}-${{ hashFiles('.pre-commit-config.yaml') }}
          restore-keys: pre-commit-${{ runner.os }}-
      - name: Install pre-commit
        run: pip install --upgrade pip pre-commit
      - name: Pre-commit (diff-only fast path)
        id: pc-diff
        continue-on-error: true
        run: |
          pre-commit run --from-ref origin/${{ github.base_ref || 'main' }} --to-ref ${{ github.sha }}
      - name: Pre-commit (fallback all-files if diff failed)
        if: steps.pc-diff.outcome == 'failure'
        run: pre-commit run --all-files

  # Standard Python Tooling (NEW)
  lint:
    needs: [lane-guard, quarantine-guard, pre-commit]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install ruff mypy black isort
      - name: Run ruff
        run: ruff check .
      - name: Run mypy
        run: mypy . --install-types --non-interactive
      - name: Check formatting
        run: |
          black --check .
          isort --check .

  test:
    needs: [lint]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11']
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run tests
        run: pytest tests/ --cov=. --cov-report=xml
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
      - name: Run bandit
        run: |
          pip install bandit
          bandit -r . -ll -f json -o bandit-report.json
      - name: Run pip-audit
        run: |
          pip install pip-audit
          pip-audit
      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2

  build:
    needs: [test, security]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: docker build -t lukhas:${{ github.sha }} .
      - name: Push to registry
        if: github.ref == 'refs/heads/main'
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker tag lukhas:${{ github.sha }} lukhasai/lukhas:latest
          docker push lukhasai/lukhas:latest
