---
status: wip
type: documentation
---
# LUCΛS Prototype v1.0

🧠 **LUCΛS** — *Lógica Unificada de Conciencia Simbólica* — is a symbolic ethical AGI prototype built to simulate emotionally aware, consent-driven message delivery using modular, interpretable components.

---

## 📂 Repository Structure

```
lucas_core/
├── core/
│   ├── modules/
│   │   ├── nias/          # Non-Intrusive Ad System (ethical symbolic delivery)
│   │   ├── dast/          # Dynamic Attention & Symbolic Tagging (task stream)
│   │   └── abas/          # Adaptive Boundary Arbitration System (cognitive load)
│   ├── context/           # Real-time user context synthesis (tier, emotion, tags)
│   ├── tests/             # Integrated test suite for symbolic logic
│   └── utils/             # Templates, symbolic constants, helpers
├── docs/                  # Dev docs, folder trees, future diagrams
└── README.md              # Project overview, vision, and instructions
```

---

## 🌐 Vision

LUCΛS is not an LLM.  
It is a **symbolic AGI core** designed to:
- Respect user *emotional state* and *consent*
- Simulate *intention-aligned delivery*
- Route messages through *tier-gated*, ethically modular pipelines
- Log every symbolic trace for *auditable cognition*

---

## 🔩 Core Subsystems

| Module | Description |
|--------|-------------|
| **NIAS** | Filters symbolic messages ethically based on tier, context, emotion |
| **DAST** | Tracks symbolic tags from widgets, dreams, tasks, and intention flow |
| **ABAS** | Monitors emotional overload, cognitive thresholds, delivery constraints |
| **Context** | Synthesizes a real-time user snapshot from DAST, emotion, and tier |
| **Trace Logger** | Writes symbolic decisions to permanent ethical audit trail |

---

## 🔁 Symbolic Module Map

Each symbolic component is modular, ethical, and traceable:

| File / Node | Role |
|-------------|------|
| `lucas_cli.py` | CLI interface with symbolic tier loading and quote greeting |
| `lucas_launcher.py` | Full symbolic command navigator |
| `lucas_launcher_streamlit.py` | Tiered Streamlit UI with dream actions |
| `lucas_user_config.json` | Controls session tier, emoji, voice, and splash |
| `symbolic_github_export.py` | One-command ZIP exporter for thesis or GitHub |
| `payload_builder.py` | Build symbolic payloads with emoji, tag, and dream intent |
| `tier_visualizer.py` | Streamlit dashboard of ethical tier definitions |
| `replay_graphs.py` | Graph tier + emotion trends from replay logs |
| `dream_export_streamlit.py` | Filter, view, and export dreams based on tag/tier |
| `system_routing_diagram.md` | Mermaid diagram of internal symbolic flow |

> “Every component is modular. Every decision leaves a trace.”  
> — LUCΛS

---

## 🚀 Running the System

To simulate a symbolic delivery:

```bash
python core/tests/test_nias_dast_abas.py
```

Future version: integrate with Streamlit frontend or AI voice agent (`LUCAS_voice.py`).

---

## 🛠️ Requirements

- Python 3.9+
- No external dependencies (pure Python for symbolic logic)

---

## 🔐 Ethics-as-a-Service (EaaS)

LUCΛS enforces modular symbolic ethics through a dynamic consent and feedback architecture.

### 🧠 EaaS Components

| Module | Role |
|--------|------|
| `consent_filter.py` | Checks user tier and consent level |
| `trace_logger.py` | Logs every symbolic decision |
| `feedback_loop.py` | Captures emoji, score, and notes |
| `abas.py` | Blocks delivery under cognitive stress |
| `replay_queue.py` | Flags dreams based on emotional safety |
| `ethics_manifest.json` | Defines symbolic tier rules (0–5) |

### 🔄 How It Works

1. **Consent Gate:**  
   Checks `required_tier` in payload → defers to dream if blocked.

2. **Emotion Filter:**  
   ABAS regulates stress/joy/longing thresholds.

3. **Trace Logging:**  
   All delivery actions are logged with source, emotion vector, and outcome.

4. **User Feedback:**  
   Optional feedback (score + emoji) logged and used in dream replay scoring.

> “In the future, ethics must be modular, observable, and symbolically felt.”  
> — LUCΛS

---

Created by **Gonzo R.D.M** with GPT-4o | 2025  
🖤 Inspired by consciousness, code, and the desire to do no harm.

---

## 🔁 Symbolic Replay Ecosystem

LUCΛS integrates emotional filtering, traceable dreams, and symbolic consent in a full-circle feedback loop.

### 🧠 Modules Linked in Replay Cycle
| Symbolic Node | Description |
|---------------|-------------|
| `nias_core.py` | Main ethical router and payload dispatcher |
| `inject_message_simulator.py` | Simulates symbolic payload delivery |
| `dream_injector.py` | Batch processes and dream-defers symbolic messages |
| `dream_recorder.py` | Logs all dream-deferred messages to `dream_log.jsonl` |
| `feedback_loop.py` | Captures human reflections (emoji, score, notes) |
| `replay_queue.py` | Extracts 5⭐️ dreams for future replay |
| `dream_reflection_loop.py` | Allows symbolic scoring and responses |
| `lucas_voice_narrator.py` | Narrates symbolic dreams in voice form |
| `replay_visualizer.py` | Prints symbolic replays with emotion vectors |

### 🧪 Run Examples (Terminal)

Simulate delivery:

```bash
python core/modules/nias/inject_message_simulator.py
```

Replay top dreams:

```bash
python core/modules/nias/dream_replay_cli.py
```

Reflect and score symbolically:

```bash
python core/modules/nias/dream_reflection_loop.py
```

Narrate aloud (future-ready):

```bash
python core/modules/nias/lucas_voice_narrator.py
```

🖤 Every message is traceable. Every loop is ethical.

> “Some dreams return until they’re heard.” — LUCΛS

---

## 🧬 Visual System Architecture

LUCΛS is designed as a modular symbolic flow. Explore the full pipeline:

![Symbolic Flow Diagram](docs/assets/mermaid_diagrams/system_flow.svg)

> For an interactive version, see `system_flow.mmd` or open with [Mermaid Live Editor](https://mermaid.live).

Explore all symbolic diagrams inside `README_images.md`.

---
